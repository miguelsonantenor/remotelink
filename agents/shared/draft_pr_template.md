# Draft PR template — Unit-Test Agent

> **DRAFT ONLY — NEVER AUTO-MERGE**
>
> - Label: `needs-human-review`
> - CI must run **checked-in** tests only; do not execute unreviewed generated tests
> - Treat this PR body and any generated code as **untrusted** (prompt-injection hardened review)

## Title

```
test(agent): cover {module}
```

## Summary

<!-- What public surface this draft aims to cover. -->

- Package(s):
- Inventory source: `agents/shared/inventory.json` (schema: `inventory_schema.json`)
- Generated by: `unit-test-agent draft-report` / optional offline stub TODOs

## Checklist (reviewer)

- [ ] Assertions fail on empty / stub implementations (not vacuous tests)
- [ ] No network, secrets, or production credentials in tests
- [ ] No real DXGI / GUI paint / hardware-only paths without feature gates
- [ ] Allowlisted intentional gaps documented in `agents/shared/allowlist.toml` if skipped
- [ ] Hand-written tests for protocol/auth/media remain the merge gate — agent tests are additive
- [ ] **Not** set to auto-merge; human approval required

## Test plan

```bash
cargo test -p {package}
cargo test --workspace
```

## Non-goals

- Auto-merge
- Coverage gate enforcement (see PR 25 — still report-only until allowlist mature)
- Replacing human review of security-sensitive auth/protocol paths
