[package]
name = "coverage-gate"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Per-package coverage / test-presence gates (fail-closed allowlist only)"
publish = false

[[bin]]
name = "coverage-gate"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
tempfile = "3"
